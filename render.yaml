services:
  - type: worker
    name: discord-role-bot
    runtime: docker
    repo: https://github.com/PashaSudak/DiscordBot
    branch: main
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DISCORD_TOKEN
        sync: false